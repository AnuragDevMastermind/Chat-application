export enum LOADING_STATUS {
  IDLE = "idle",
  LOADING = "loading",
}

export enum SNACKBAR_STATUS {
  IDLE = "idle",
  ERROR = "error",
  SUCCESS = "success",
}

export enum SIDEBAR_STATUS {
  DEFAULT = "default",
  CONTACTS = "contacts",
  SETTINGS = "settings",
}
